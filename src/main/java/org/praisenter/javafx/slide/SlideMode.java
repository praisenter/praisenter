package org.praisenter.javafx.slide;

// TODO we may need a PREVIEW option too for previewing a slide's animation and video etc, but muted for example

public enum SlideMode {
	EDIT,
	SNAPSHOT,
	PREVIEW,
	PRESENT,
	MUSICIAN
}
